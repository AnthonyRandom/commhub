# Docker Compose Environment Variables
# ==========================================
# Copy this file to .env in the root directory
# Command: cp docker.env.example .env
#
# IMPORTANT: Update all values marked as "CHANGE_THIS" before deploying
# ==========================================

# ======================
# DATABASE CONFIGURATION
# ======================
# PostgreSQL database name
POSTGRES_DB=commhub

# PostgreSQL username
POSTGRES_USER=commhub_user

# REQUIRED: PostgreSQL password
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD

# =================
# JWT CONFIGURATION
# =================
# REQUIRED: Secret key for JWT token signing
# Generate with: openssl rand -base64 64
# WARNING: This MUST be set for security. Never use default values!
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_JWT_SECRET

# =====================
# SERVER CONFIGURATION
# =====================
# Server port (default: 3000)
PORT=3000

# Node environment
NODE_ENV=production

# ===================
# CORS CONFIGURATION
# ===================
# Comma-separated list of allowed origins
# Update with your actual frontend URLs in production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:1420

# ===================
# HTTPS CONFIGURATION
# ===================
# Enable HTTPS (requires SSL certificates)
HTTPS_ENABLED=false

# SSL certificate paths (only if HTTPS_ENABLED=true)
# SSL_KEY_PATH=/path/to/ssl/key.pem
# SSL_CERT_PATH=/path/to/ssl/cert.pem

